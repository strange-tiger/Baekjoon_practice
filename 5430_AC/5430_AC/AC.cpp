#include <iostream>
#include <string>
using namespace std;

int T, n, x;
string cmd;

int main()
{
	ios_base::sync_with_stdio(false);
	cin.tie(nullptr);
	cout.tie(nullptr);

	cin >> T;
	while (T--)
	{
		cin >> cmd;
		cin >> n;
		for (int i = 0; i < n; ++i)
		{
			cin >> x;
		}

	}

	return 0;
}